import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: true,
  output: "export",
  basePath: "/egresi-kata-cosmetics",
  assetPrefix: "/egresi-kata-cosmetics",
};

export default nextConfig;
